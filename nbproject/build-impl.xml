<?xml version="1.0" encoding="UTF-8"?>
<project name="SistemaGestionOrganizacional-impl" default="default" basedir="..">
    <property file="nbproject/private/private.properties"/>
    <property file="nbproject/project.properties"/>

    <path id="compile.classpath">
        <pathelement location="${file.reference.mysql-connector-j.jar}"/>
    </path>

    <target name="-init">
        <mkdir dir="${build.classes.dir}"/>
        <mkdir dir="${dist.dir}"/>
    </target>

    <target name="compile" depends="-init">
        <javac srcdir="${src.dir}" destdir="${build.classes.dir}"
               source="${javac.source}" target="${javac.target}"
               encoding="${source.encoding}" includeantruntime="false"
               debug="true">
            <classpath refid="compile.classpath"/>
        </javac>
        <copy todir="${build.classes.dir}">
            <fileset dir="${src.dir}" excludes="**/*.java,**/*.form"/>
        </copy>
    </target>

    <target name="jar" depends="compile">
        <jar destfile="${dist.jar}" basedir="${build.classes.dir}"
             manifest="${manifest.file}" compress="${jar.compress}"/>
        <mkdir dir="${dist.dir}/lib"/>
        <copy todir="${dist.dir}/lib">
            <fileset dir="lib" includes="*.jar"/>
        </copy>
        <copy todir="${dist.dir}/database">
            <fileset dir="database" includes="*.sql"/>
        </copy>
        <copy file="settings.properties" todir="${dist.dir}"/>
    </target>

    <target name="run" depends="compile">
        <java classname="${main.class}" fork="true" failonerror="true">
            <classpath>
                <path refid="compile.classpath"/>
                <pathelement location="${build.classes.dir}"/>
            </classpath>
            <jvmarg line="${run.jvmargs}"/>
        </java>
    </target>

    <target name="test" depends="compile">
        <mkdir dir="${build.test.classes.dir}"/>
        <javac srcdir="${test.src.dir}" destdir="${build.test.classes.dir}"
               source="${javac.source}" target="${javac.target}"
               encoding="${source.encoding}" includeantruntime="false">
            <classpath>
                <path refid="compile.classpath"/>
                <pathelement location="${build.classes.dir}"/>
            </classpath>
        </javac>
        <java classname="com.gestionorganizacional.IntegrationTest" fork="true" failonerror="true">
            <classpath>
                <path refid="compile.classpath"/>
                <pathelement location="${build.classes.dir}"/>
                <pathelement location="${build.test.classes.dir}"/>
            </classpath>
        </java>
    </target>

    <target name="clean">
        <delete dir="${build.dir}"/>
        <delete dir="${dist.dir}"/>
    </target>

    <target name="default" depends="jar"/>
</project>
